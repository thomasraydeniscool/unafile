export default async (data: Buffer): Promise<Buffer> => {
  /**
   * TODO: Convert docx to pdf
   * TIP: use unoconv (this requires machine installed dependencies)
   */
  return Buffer.from(null);
};
